# Documentation

This folder is a separate Git repository for project documentation.

Main project commits automatically commit changes in this folder through `.githooks/post-commit`.
Main project pushes automatically push this repository through `.githooks/pre-push` after an `origin` remote is configured here.
